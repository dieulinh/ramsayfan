- if @dishes.any?
	h1 My Dish
	table.table.table-bordered.dishes#index
		thead
			tr
				th Title
				th Desription
				th Publish
				th Cost
				th Pax
				th Vegetarian
				th Actions
			tbody
			- @dishes.each do |dish|
				tr 
					td
						= link_to dish.title, dish_path(dish)	
					td = dish.description
					td.text-center
						i class=(dish.published? ? 'fa fa-check' : 'fa fa-close')
					td = number_to_currency dish.cost
					td = dish.pax
					td.text-center
						i class=(dish.vegetarian? ? 'fa fa-check' : 'fa fa-close')
					td 
						.btn.btn-default
							= link_to "Details", dish_path(dish)
						.btn.btn-default
							= link_to "Edit", edit_dish_path(dish)
						.btn.btn-default
							= link_to (dish.published? ? "Unpublish" : "Publish"), user_publish_path(dish), method: :put
- else
		.alert.alert-info 
			| No dish found
.btn.btn-default
	= link_to "Back", :back

